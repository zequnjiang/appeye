# 2026-09-07 六国财务应用发现与完整采集任务

- 状态：第一项“六国发现、详情分析与信贷入库”及双店2,024条市场记录的14,168项七类补充处理已通过Alex全量核查和PM阶段验收；12项Apple来源失败及2项GP部分目录分别保留。全部可分页评论继续执行，整体尚未达到最终验收条件。
- 类型：一次性运营采集，不是新的后台产品或界面需求。
- 目标日期：2026-09-07（Asia/Shanghai）。实际执行开始、结束与恢复时间使用 ISO UTC 另行记录，跨日执行不伪装在同一天完成。
- GitHub：[执行与验收议题 #11](https://github.com/zequnjiang/appeye/issues/11)、[草稿PR #12](https://github.com/zequnjiang/appeye/pull/12)，工作分支 `codex/full-finance-scan`。工程/方法已完成[PM阶段验收](../reports/FULL-SCAN-PM-ACCEPTANCE.md)，整体运营尚未验收，#11继续打开。
- 依赖：[v0.2 需求与数据口径](V0.2.md)、[GitHub 工作流](../workflow.md)。
- 执行记录：[阶段运营报告](../reports/FULL-SCAN-EXECUTION.md)。批次`finance-2026-09-07`，下列范围与验收标准继续有效，不因任务耗时而缩小。

## 目标与范围

用户要求扫描泰国、墨西哥、菲律宾、巴基斯坦、印度尼西亚、阿根廷的财务类应用，识别信贷并保存；对所有已经入库的应用尽可能采集完整信息，有多少可取得信息就保存多少。

本项目按`country/store/externalId`跟踪市场记录；同一商店应用在不同国家会形成多条记录。运营报告同时给出市场记录数和跨国家按`store/externalId`去重的商店App身份数，不把2,024条市场记录描述为全球2,024个不同产品，也不将跨商店同品牌自动合并。

本轮将两个任务分开计数：**市场发现**采用六国两商店可访问的财务榜单和当地关键词，完整发现集合及已取得详情先保存到批次暂存记录（staging），再按信贷证据决定新增主应用库记录；**资料补齐**覆盖启动时已入库应用及本次新增主应用库记录，不因分类而跳过。新增发现和后续采集可分批运行，但必须使用同一运营批次标识与可恢复进度。

Google Play 和 App Store 的公开榜单、搜索与采集库并不提供可证明完整的商店应用枚举。因此，本轮“扫描完成”只表示已完成记录中的可获取发现来源及应用资料采集计划，不表示已找到这些国家的全部财务或信贷应用，也不用于推算完整市场份额。

## 1. 执行清单

CEO 在执行记录中固定批次标识、开始时间、运行依赖/代码版本、分类规则版本及数据库备份或恢复点。CTO 核实两个维护版库的实际列表、搜索、评论及补充方法，记录可用参数、分页机制、已知上限和不支持项，不从旧库行为推断新库能力。

| 项目 | 本轮范围 |
| --- | --- |
| 发现国家 | th、mx、ph、pk、id、ar。 |
| 发现商店 | google-play、app-store。 |
| 榜单 | 每个国家/商店实际支持的 Finance / 财务分类榜单；记录具体collection、榜型、分类标识和可访问条数/页数。不可访问的组合有实际失败或不支持记录。 |
| 搜索 | 保存执行时六国的当地语言和借贷关键词清单，按实际接口可取得范围继续分页或请求。关键词命中形成样本，不解释为分类全量。 |
| 发现暂存 | 保存所有去重发现及已获取详情、原始返回、识别结果和来源；证据不足的新对象保留在staging，不因未进入主应用库而丢弃已取得信息。 |
| 资料补齐对象 | 启动时全库已有App，加上本次发现新增App；按数据库ID及国家/商店/externalId建立清单。包括confirmed、candidate、excluded、manual和legacy。 |
| 新国家与额外接口 | 不为本次任务扩展新的发现国家或产品界面；不要求递归发现每个关联应用。已经存在的其他国家记录若在全库清单内，同样执行资料补齐。 |

以本轮发现全部结束后的主应用库清单作为最终资料覆盖分母；仅在staging的对象单独统计，不计入“所有已入库App”的补齐分母。执行期间正常后台任务新增的应用应明确计入本次清单或独立记录，不能使分母随统计口径悄然改变。不能因某国暂停自动周期而遗漏本轮清单中已存在的应用。

本轮发现详情任务覆盖“全部唯一发现对象∪原主库App”，其分母与主库补充/评论分母不同。来源阶段已得到6,495个唯一发现对象，命中474个原有App中的460个，另14个原有App仍须采集，因此详情任务分母为6,509；不能把这6,509个对象都称为主库App或已识别信贷。现6,509详情与分析全部完成，新增主库1,550（407 auto confirmed、1,143 auto candidate），加原474个App后，本轮第二项主库分母冻结为2,024；七类补充14,168项，首评论流2,024个。4,485个新insufficient对象仅保留staging全部资料，不计入主库补齐分母。

### App Store 搜索的已核实边界

PM于2026-09-07读取[Apple官方存档：Constructing Searches](https://developer.apple.com/library/archive/documentation/AudioVideo/Conceptual/iTuneSearchAPI/Searching.html)（页面更新日期2017-09-19）。该存档写明搜索`limit`为1–200、默认50；`country`指定店面，搜索返回语言`lang`仅列`en_us`与`ja_jp`，默认英语；Search API约每分钟20次调用且可能变化。它是接口参考，不是新的法律政策，也不能替代本轮真实调用结果。

本轮App Store每关键词按最多4批×50条处理至多200条公开搜索结果。批次、上游实际请求和分页机制分别记录；若库将一次返回在本地切分，应记为本地切分，不能声称存在4个独立上游游标页。当地语言关键词、请求店面、实际搜索`lang`与评论请求语言分别保存；使用当地词语搜索不证明搜索返回或评论均为该国语言。

运行计划对iTunes `/search` 与 `/lookup`合并设置至少3100ms请求间隔，其他请求设置至少500ms全局间隔，并计入库内部请求。前者是本轮依据官方存档采用的保守执行设置，后者是本地执行配置；二者都不是“商店保证不会限流”的承诺，遇实际限流仍要退避并保留进度。

## 2. 信贷识别与人工覆盖

沿用已验收的 `strong / possible / insufficient` 识别方法和人工优先规则，不在扫描脚本中另外发明一套信贷标签：

- 本轮扫描中新发现且主应用库尚不存在的对象，strong以auto模式进入主应用库并成为confirmed；possible进入主应用库并成为candidate；insufficient仅保留staging及全部已取得资料，不新建主应用库App。Finance分类或loan关键词本身不是确认信贷的证据。此处是一次性批次的入库筛选，不改变v0.2普通新增App的默认分类规则。
- 原有manual与legacy的有效classification全部保留。旧应用即使新分析为strong，也只更新自动建议，不自动解除人工覆盖。
- 人工excluded不会重新变成confirmed，也不会因本轮搜索再次命中而撤销排除；但用户要求所有入库App补齐资料，因此excluded仍要尝试数据采集。
- 主应用库原有App即使本轮分析为insufficient也保留并执行全部补齐任务，不能迁出主库或缩小覆盖分母。
- 报告分别统计发现暂存总数、暂存未入库数、有效confirmed、自动strong建议、人工/legacy覆盖以及新增候选；不得把这几种数量混称为“新发现信贷”。
- 版本化自动分析与原文观测时间分开记录。对已有数据重算规则不制造新的商店快照或首次发现日期。

本轮不得为了提高“识别率”批量解除人工覆盖、删除主库弱证据应用或暂存原始资料，或将财务类应用统一改为信贷。

## 3. 所有入库应用的资料采集

每个清单内App至少具有本轮以下任务的可追溯结果；此前成功数据可以保留，但不能代替本轮尚未执行的尝试：

1. **详情**：刷新应用完整资料、当前描述、商店发布者/法律实体/seller等可用字段，保存规范字段、完整返回与原始证据；更新识别建议但尊重人工覆盖。
2. **评论**：在该国家/语言/商店接口实际支持的范围内，逐页取得全部可获取评论，不停在v0.2的近期样本默认窗口。
3. **七类补充**：permissions、dataSafety、privacy、versionHistory、inAppPurchases、ratings、developer。每种都应明确结果；商店不支持的组合登记unsupported，不捏造对应资料。

某类失败不能阻止其他资料继续采集。权限/隐私/开发者等可独立执行时，不因详情失败一概跳过；确需详情返回ID等前置条件的任务，记录依赖失败及可恢复状态，不能冒充unsupported或成功空返回。

成功返回的未知字段、嵌套数据、原始搜索行、评论及补充资料都保存，主应用库延续v0.2的可展开/分页/API能力；staging资料通过可定位的本地批次记录读取，不为此新增后台界面。不得只保留汇总条数或精选字段。资料来自商店声明，不等于实际设备权限、完整Manifest或公司/牌照核验。

### Apple详情的批量lookup

本轮采用每批最多50个ID的保守预取，减少相同接口的重复请求。参与者必须是同运营batch、同country、同冻结请求语言、尚为queued的Apple详情任务；Google Play、其他批次或其他国家不能混入。实际Apple请求`lang=en_us`，仍保留冻结语言上下文，不宣称商店响应是当地语言。

合格结果按精确trackId一一匹配，使用指定维护库的相同字段映射；每个App仍有独立detail任务、attempt、结果与快照。单App保存自己的完整原始lookup行（包含seller与未知字段），并用`httpId`引用一次真实批量HTTP全响应。快照与来源记录使用该真实请求的fetchedAt，不以逐App处理或缓存重放时刻伪装新观测，不把另外49个App原行复制到当前App快照。

缺少目标ID、重复ID、三类截图均无数据、批量失败或不能安全解析时回退原单App采集；保存已取得批量原响应及回退原因，不用不完整行宣称完整详情。该优化不改变入库标准、人工覆盖、七类补充或评论义务；中断后未完成项可重取，已完成任务与持久来源保持可追溯。

## 4. 评论分页与终止原因

CTO 依据实际库能力记录每个商店的分页方式、排序、语言和页大小。对每个应用/国家/语言流保存页号或cursor、请求时间、返回数、新增稳定ID数、更新数、错误和下一步位置。

| 结果 | 何时可采用 | 验收含义 |
| --- | --- | --- |
| 自然结束 | 上游明确无next cursor/hasNext，或按已核实接口契约返回终止空页。 | 当前接口上下文可获取评论已遍历，不能扩张为全商店/所有语言历史评论。 |
| 上游限制 | 已证明存在最大页数、返回窗口、地区可见性或接口不提供继续位置。 | 标注具体方法、上限/现象和实际覆盖；可按已知限制完成该来源，不声称全量。 |
| 无进展保护 | cursor重复或连续重复页，无法安全继续。 | 保存已取得资料和最后位置，标明保护停止；不自动冒充自然结束，需复核接口行为。 |
| 暂时失败 | 网络、限流、超时或可恢复结构异常。 | 有限重试及退避，保留上次成功、失败页和恢复游标；未解决时保持待恢复或明确失败。 |
| 运行切片/进程中断 | 本轮执行分批、重启或停止。 | 持久保存最后成功页和下一步位置，继续时幂等恢复。它不是该App评论采集完成。 |

禁止人为设置“每App前N页”“只取最新100条”后将其记为完整完成。可以分批处理以控制单次运行时间，但未完成页须留在队列/检查点并继续。若上游库本身有真实上限，报告真实能力边界，不能把本地切片上限说成商店上限。

稳定评论ID按跟踪App去重；重复页或恢复重放不增加重复评论。评论修改可以更新既有记录，新增数与更新数分开。若上游无稳定ID，沿用带来源说明的内容标识，明确其限制。评分人数和商店聚合评论数量不能直接当作公开接口必须返回的文本评论总数。

## 5. 运行与恢复

- 使用现有单主机数据库和单活动worker边界，避免运营脚本与后台worker同时争抢同一任务或并行写入不兼容的游标。
- 请求遵守实际限速，包含库内部请求；遇429、超时等保存错误并有限退避。不能以无界并发换取完成速度。
- 任务及分页进度持久化。重启后可从最后成功位置恢复；游标敏感时留在被忽略的本地数据中，GitHub报告只记录存在性/摘要，不公开访问凭据。
- 失败不覆盖最近成功数据、时间和上下文；最新尝试与上次成功分开。成功空返回、unsupported、依赖失败、未尝试和普通失败不得混同。
- 本轮不改国家关键词、采集周期或用户分类偏好。运行必要的暂停/恢复需记录实际状态，结束时恢复原先运行设置。
- 真实库、备份和大体积原始资料留在受控本地存储；GitHub提交需求、方法、统计、脱敏错误与验收证据，不提交凭据或真实评论正文/用户名。

### 已证公开来源不可获取的终止口径

`source-unavailable`是本轮报告对失败原因的归类，不要求新增任务状态或后台功能。它表示在明确的时间、店面、URL和采集方法范围内，公开来源无法取得；并不意味着该来源永久失效、商店没有此项资料或App已下架。现有任务应保留`failed`及具体错误，不能为了结束批次改成`empty`、`unsupported`或`succeeded`。

以下条件全部有证据后，PM才可将相应失败项作为本轮已列来源限制接受。条件对应FS-AC-07/08/09/10的失败、留存、有限恢复与报告义务，不能将一般失败批量归入外部限制。

| 编号 | 接受来源受限终止的条件 |
| --- | --- |
| FS-SU-01 范围明确 | 逐项固定batch、App、国家/商店、补充类型、任务ID、请求URL和时间。仅适用于实际依赖失败来源的操作，不能扩大为整个App、整个商店或同国全部应用不可获取。 |
| FS-SU-02 上游证据 | 保存真实官方来源状态、Location链与循环/终止条件。重定向循环须有相同URL再次指向自身或已访问URL的证据；笼统`fetch failed`单独不足。合理的官方canonical/lookup URL核对有界进行，不以无穷路径猜测要求永久重试。诊断探测明确不作为成功入库响应。 |
| FS-SU-03 排除内部原因 | 已核对国家/ID/接口路由正确，已知SDK或本地实现缺陷分别修复/记录。保留可用的底层cause；内部构造错URL、解析失败、漏数据或未执行不能当上游不可达。若修复或新官方入口已可恢复，继续采集，不采用本终止口径。 |
| FS-SU-04 有限真实重试 | 实际任务完成预先规定的有限重试及退避；修复/诊断加强后如安排新有限尝试，保留原attempt序号、历史和新预算，不抹掉过去失败。次数不统一要求6次：已启用原因记录的原3次若每次均有明确cause，且该国家官方来源链、正确路由及其他条件齐备，可按实际有限3次审查；初始六项的6次仅是诊断部署前后两轮事实。同App另一国家不可借用原国家证据直接接受。新尝试尚排队、运行或还有可执行恢复步骤时，暂不作终止验收。 |
| FS-SU-05 状态与原资料 | 失败任务计入“已尝试/失败”，不计入“成功取得”或“成功空”。本轮未取得HTML或结构化数据就明确标未取得；保留原有成功资料、原观测时间和来源，不能清空或伪造本轮成功。详情/lookup及其他可用接口继续各自采集。 |
| FS-SU-06 独立验证 | CTO提交原因/重试证据，Alex只读核对每项最后attempt、原/新历史、请求上下文和底层错误，PM决定是否接受为本轮来源限制。错误发生时间、诊断时间与新重试时间分别列明。 |
| FS-SU-07 完成表达 | 限制清单写“本轮某App某类型因已证官方来源循环未能取得；有限采集尝试已终止”，保留后续可重新检查的身份与请求方法。只有其他必需任务全部完成，才可称整体“按所列来源限制完成”；没有通过本条件的失败或仍排队/待续对象继续算未完成。 |

已按上述条件接受的范围为12项：原App1060（ph/App Store，871608181）与App1172（pk/App Store，1089271220）各3类型、每项累计6次；新增App1638（mx）与App1819（ph）的Apple1089271220各3类型、每项累计3次。类型均为privacy、versionHistory、inAppPurchases。四条市场记录涉及两个不同Apple商店ID，三个官方探测文件按各国范围使用；54条实际attempt/HTTP/history和36条诊断增强后明确cause经Alex/PM各自只读核对，详见[十二项来源限制PM验收](../reports/APPLE-SOURCE-LIMIT-PM.md)。仅接受为本轮来源限制并保留failed，未取得资料不得称成功；详情/ratings/developer仍成功。该现象与[#14数值开发者ID路由缺陷](https://github.com/zequnjiang/appeye/issues/14)分开，不合并根因。双店七类补充处理已通过阶段验收，评论及整体任务仍未完成。

### BCA开发者源错误的单轮定向恢复

这是[#11](https://github.com/zequnjiang/appeye/issues/11)现有完整采集范围内的极小恢复要求，不新增产品界面或总目标。当前已知印度尼西亚BCA任务16108/App1182、16116/App1183均有初始10项目录；续页HTTP7232/7236虽为200，但`qnKhOb`的payload为null，错误结构为`PlayDataError`、code5（已观察detail为`[1]`），没有可用于#13兼容转换的应用数组。不能解释code5未公开核实的业务含义，也不能把10项说成完整目录。

本次使用一次新的显式`--retry-developer-source-errors`，原`--retry-warnings`仅负责原有已证解析布局恢复，不扩大到本源错误。现runtime-5已经受控运行，CTO→Alex120项回归/真实审计→PM独立核查完成，详见[单轮恢复及#15验收](../reports/DEVELOPER-SOURCE-ERROR-PM.md)。两项均只恢复一轮，仍部分10项+source5；已接受为本轮部分目录续页来源限制，继续保留developer-degraded而非完整成功。其后双店补充处理已通过阶段验收，全部可分页评论仍待完成。

| 编号 | 可验收行为 |
| --- | --- |
| FS-DR-01 严格候选 | 只选择当前batch、Google Play、developer、succeeded且developer-degraded的任务；最后一条相应续页HTTP必须属于该task/batch、HTTP200，并严格正判已知qnKhOb的null payload与PlayDataError code5结构。仅早页匹配、错误RPC/代码、未知结构、正常数据、普通失败或其他类型/批次均不匹配。当前目标16108/16116，实际选中清单应固定并可审计。 |
| FS-DR-02 显式独立入口 | 只有新显式开关触发本轮重排；普通启动、原retry-warnings及其他重试范围不自动扩大。记录本轮恢复标识、原因、原末页HTTP引用、原累计attempt基数；中断恢复接续这一轮，不自动开启无限新轮或循环重置预算。 |
| FS-DR-03 历史保护 | 复用既有历史保护事务，保留旧完整response、原HTTP正文/获取时间、attempt与enrichment_history；累计attempt序号延续。新响应和HTTP另存，payload的原因/旧HTTP引用足以还原为何选中，不能删除旧10項目录或抹去旧warning。 |
| FS-DR-04 有限重试 | 本轮按已存在的maxAttempts=3给予有限新预算，网络/超时等失败按原退避机制处理；预算耗尽则failed并保留此前成功的部分资料。若一次有效响应再次返回初页部分目录加同source5，则该轮立即结束，不因仍有同warning而自动再执行另一轮。 |
| FS-DR-05 部分结果语义 | 再次取得部分目录且带同source5时，完整新response和原HTTP先持久化，沿用succeeded+developer-degraded并保留warnings及实际取得条数。这里succeeded只表示本次操作产生了可保存的部分返回，必须单列“部分取得/有完整性告警”，不可计为完整开发者目录成功、不可清warning或声称自然结束。新观测时间使用实际新请求，旧部分资料/旧时间仍在历史。 |
| FS-DR-06 正常恢复与未知异常 | 只有真实无告警的完整目录才按正常available及无完整性告警记录恢复；未知新布局、错误码或循环仍显式待复核，不因本补丁改为成功空/不支持/来源终止。其他已验收结果、分类、国家配置和所有评论任务保持原范围。 |
| FS-DR-07 独立验证 | Alex验证严格筛选、旧开关隔离、一轮/预算/中断恢复、部分结果不假完整、旧新原文与时间、正常完整恢复及网络失败保旧成功。真实执行后再核目标任务的新attempt、源HTTP、历史和剩余告警；PM仅据实际结果验收恢复或单项来源限制，不据工程fixture提前接受整体完成。 |

本小节与Apple完全未取得HTML的失败语义分开：Apple十二项仍failed且无成功资料；BCA已确实取得部分目录，可以保留已有succeeded/degraded状态，但统计和验收必须同时读取warning/stop_reason，不能只看status。PM接受本轮源限制后仍须继续显示“部分目录，续页源错误”，不得把该目录标为已完整取得。2,024市场记录的全量评论义务不因这次一轮恢复缩减。

### 补充资料响应与重放一致性（#15）

[#15](https://github.com/zequnjiang/appeye/issues/15)是CTO离线快速恢复测试发现的幂等缺陷：仅按App/类型/fetchedAt判断已应用，会把同一毫秒的不同响应误认为重放，出现任务成功而补充仍旧failed，或漏存新部分结果。真实批次当前未发现该数据损失；不能将离线复现写成已发生的线上事故。本项纳入runtime-5前的CTO→Alex→PM验收，不改变扫描或评论范围。

- 冻结同一毫秒，旧失败→新成功、旧部分结果→不同新部分结果，均按真实新响应/尝试独立保存，任务和当前补充结果一致。不同真实尝试即使内容相同，也不能仅因时间或内容相同被吞掉。
- 同一已保存响应的中断恢复只应用一次，不新增重复history，不因新恢复attempt或当前时间制造一份新市场观测；必须能区分持久响应身份与新的执行尝试。
- 原raw/HTTP/attempt/history、真实观测时间及旧成功资料保持；失败仍不清空最近成功数据。若采用额外应用标识，应与资料写入保持事务一致，沿用Store现有事务边界，不引入嵌套事务或“标记已应用但未写资料”的恢复漏洞。
- Alex独立冻结时间与中断回归须覆盖上述边界。现已在120项完整检查及本次专项中通过，runtime-5真实新响应/应用标记/历史也已核对，PM已给出[#15通过结论](../reports/DEVELOPER-SOURCE-ERROR-PM.md)；它不构成整个运营完成。

## 6. 验收标准

| 编号 | 级别 | 可核对结果 |
| --- | --- | --- |
| FS-AC-01 | P0 | 六国×两商店的来源计划齐备，财务榜单及当地搜索有实际执行证据、时间、参数、条数和终止/限制原因；缺失来源明确标注，不宣称商店全量。 |
| FS-AC-02 | P0 | 发现结果按country/store/externalId去重，全部已取得原始行及详情保存到可定位批次记录；新strong入主库为auto confirmed、新possible入主库为auto candidate、新insufficient留staging；Finance与关键词不直接等同信贷，暂存数与主库数分开。 |
| FS-AC-03 | P0 | 原manual/legacy/excluded分类逐条保持，覆盖不被自动解除；有效分类和自动建议单独统计；excluded仍进入资料补齐。 |
| FS-AC-04 | P0 | 有启动主库清单、本次新增主库App及最终分母；仅staging的新insufficient不计入补齐分母，原主库insufficient/excluded仍计入。每个清单App均有本轮详情尝试及结果，失败有可解释原因，不以旧成功代替本轮执行。 |
| FS-AC-05 | P0 | 每个App评论流按实际可用分页继续，记录覆盖/游标/终止原因；本地运行切片不误报结束，无尚可继续却被静默丢弃的页。 |
| FS-AC-06 | P0 | 评论恢复/重复页幂等，稳定ID无重复；新增/更新/总留存数区分；缺ID限制、不同请求语言和上游返回上限准确说明。 |
| FS-AC-07 | P0 | 每个清单App的七类补充均有本轮available/empty/unsupported/failed或依赖阻塞记录；任何失败不静默吞掉，不清空旧成功。 |
| FS-AC-08 | P0 | 所有已成功取得响应及未知字段持久化，主库原始资料可通过既有详情/API/分页读取，staging原始资料可通过批次记录读取；权限和主体来源不被错误认证。 |
| FS-AC-09 | P0 | 单worker、限速和有限重试运行；中断/限流/失败有持久恢复点；恢复不重复创建App或破坏历史；结束恢复原运行设置。 |
| FS-AC-10 | P0 | 结束报告逐国/逐商店列发现、分类、详情/评论/补充覆盖、成功空、不支持、失败和未尝试；计数可追溯且分母一致。 |
| FS-AC-11 | P0 | CTO执行/自检、Alex独立数据与恢复验证、PM最终验收齐备，关联本轮GitHub议题。未解决内部缺陷、未尝试对象或可继续分页仍有剩余时不能宣称全任务完成。 |

## 7. 完成与报告口径

最终报告至少包含：

- 批次标识、开始/结束时间、依赖/代码/识别规则版本、真实数据集和恢复记录。
- 六国两商店来源覆盖矩阵，榜单/搜索各自的实际能力和终止原因。
- 原应用数量、发现暂存数量、暂存未入库数量、新增主库应用数量、最终主库补齐清单数量及去重规则；有效confirmed与自动strong建议分别统计。
- 对详情、评论、七类补充逐项给出已尝试、成功有数据、成功空、不支持、失败、依赖阻塞、未尝试、待继续数量。
- 评论页数、实际留存数量、自然结束/上游限制/无进展/失败/待恢复分类；已知上游限制可定位至方法与实际证据。
- 人工/legacy分类前后对照、数据库一致性、原数据未丢失以及恢复检查结果。

只有全部范围已执行、没有内部阻断和未完成恢复项时，才可写“本次可获取范围采集完成”。若剩余项全部是证据明确且无法在本轮消除的外部限制，PM可接受“按所列上游限制完成”，同时保留逐项限制。仍可继续执行的排队/分页、尚未尝试、未修复错误或仅因运行时间到达而停止的任务，结论必须是“部分完成，待继续”，并保留恢复路径。

这份任务的验收针对执行覆盖与数据留存，不对全部市场召回率、文本识别准确率、贷款牌照或法律合规作保证。CEO/CTO执行真实任务，Alex独立核对结果，PM最后给出正式结论；PM不直接修改真实数据库或任务。
