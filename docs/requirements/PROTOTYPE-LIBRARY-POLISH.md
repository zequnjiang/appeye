# 原型应用库四项调整

- 状态：**PM 最终验收通过（2026-09-09），PLP-AC-01–04 均通过；待 CEO 提交、核对精确 HEAD CI 后合并并关闭 #37。** 版本 1.0；结论见 [PM 报告](../reports/PROTOTYPE-LIBRARY-POLISH-PM.md)。
- 跟踪：[Issue #37](https://github.com/zequnjiang/appeye/issues/37)，分支 `codex/prototype-library-polish`。
- 范围：仅 `prototypes/research-workspace/` 的 4173 前端 mock 原型；沿用 [已验收原型需求](RESEARCH-PROTOTYPE.md)。不修改正式后台、API、数据库或采集，不引入真实下载量。

| 编号 | 验收标准 |
| --- | --- |
| PLP-AC-01 | 初次进入/重置应用库，默认 `minInstalls` 降序，排序控件与行序一致。先对完整筛选集合排序再分页；0 有效，空值末位，同值按记录 ID 稳定排序。GP 字段仍表示公开累计安装下限，Apple 缺失保持空值；不能称国别下载量或精确下载量。用户主动选择其他排序、详情返回和待更新清单保留已有行为。 |
| PLP-AC-02 | 每个应用行在名称旁显示对应本地图标；图标不取代名称、开发者或身份。全部初始记录、分页及模拟新增身份均有图标或明确占位，缺失/加载失败不留破图、不破坏行高与点击/选择区域；本地图例不冒称真实商店图标。桌面及 390px 查看可辨识，宽表仍在容器内滚动。 |
| PLP-AC-03 | 应用库两项日期列最终按 **商店发布日期 `releasedAt` → 商店最近更新时间 `storeUpdatedAt`** 排列，发布日期位于这两列前面。表头、每行字段及显示值相符；缺失显示“未提供”等缺失状态，不能用首次发现或采集时间替代。 |
| PLP-AC-04 | 列表不再显示旧“首次发现”日期列；原 `firstSeenAt` 数据、详情“首次发现”及可选排序保留。最近更新时间取商店字段 `storeUpdatedAt`，不取 `lastFetchedAt`。验证一个发布日期、首次发现、商店更新时间不同的示例，确认对应显示/可选排序与详情往返；原 132/120/12 固定数量和市场事件口径不因本次展示调整改变。 |

CEO 负责图标素材/CSS/实际浏览器证据/GitHub，CTO 实现并自检后交 Alex 独立回归，最后由 PM 写入 `docs/reports/PROTOTYPE-LIBRARY-POLISH-PM.md` 逐项验收。保留模拟数据标识和当前清单待点击更新规则，不扩展新功能；验收前不关闭 #37。
