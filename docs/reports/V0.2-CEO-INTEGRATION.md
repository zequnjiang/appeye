# V0.2 集成与数据迁移记录

日期：2026-09-07（Asia/Shanghai）。关联 [PR #10](https://github.com/zequnjiang/appeye/pull/10)、[需求 #7](https://github.com/zequnjiang/appeye/issues/7)、[依赖 #5](https://github.com/zequnjiang/appeye/issues/5)、[完整资料 #8](https://github.com/zequnjiang/appeye/issues/8)、[识别 #9](https://github.com/zequnjiang/appeye/issues/9)。

CEO 负责集成和最终交付；PM 维护需求和政策事实目录，CTO 实现后端及采集器并独立审查前端，Alex 编写独立回归并执行浏览器验证。规则引擎和前端由 CEO 实现。最终验收以 PM 报告为准，本文不代替独立测试与验收。

## 数据保留

停止旧服务后使用 SQLite native online backup 创建忽略提交的 `data/backups/appeye-before-v02-2026-09-07.sqlite`。首次运行 `npx tsx scripts/backfill-v02.ts`：

| 记录 | 迁移前 | 迁移后 |
| --- | ---: | ---: |
| 应用 | 474 | 474 |
| 详情快照 | 497 | 497 |
| 字段变化 | 14 | 14 |
| 评论 | 22052 | 22052 |
| 任务 | 1115 | 1115 |

474 条历史分类均设为 legacy 覆盖，474 条应用保存独立文本分析，quick_check=ok。备份及真实数据库没有提交 GitHub。

规则边界修复完成后，再次停止唯一 worker，以 `--force-analysis` 重算最终 `2026-09-07.1/heuristics-2`。此次前后均为 474 应用、502 快照、22 变化、26876 评论、1121 任务；与首次迁移的差异来自其间真实采集，重算本身不发起网络也不生成观测。对最初备份逐条比较全部 474 个 ID 的分类，**差异为 0，全部仍为 legacy+override**。全库 474 条分析版本一致，quick_check=ok。

## 正式库中的真实补充采集

除 CTO 独立 smoke 外，CEO 在暂停 worker 时用新适配器刷新正式库两个既有泰国样本：

- Google Play `com.kredivo.th.android`：57 个商店详情字段；developerLegalName 为 `KREDIVO (THAILAND) COMPANY LIMITED`；公开权限 20 项；dataSafety 与开发者目录成功。Apple 专属方法明确为 unsupported。
- App Store `6748000182`：35 个返回字段并保留原始 iTunes JSON；sellerName 为 `Kredivo (Thailand) Company Limited`；privacy、ratings、开发者目录成功；版本历史 15 条；inAppPurchases 返回空列表；Android 权限不支持。

资料、原始响应及每次尝试均写入正式数据库。所述公司为商店声明，未进行工商或金融牌照核验。权限为商店公开返回，不是实际授权或 Manifest 检查。其他真实调用与失败修复见 [CTO smoke](V0.2-LIVE-SMOKE.json)。

## 集成检查与运行

- `npm run check`：61/61 确定性测试通过，前后端 TypeScript、生产构建通过。
- 完整 `npm audit --json`：info/low/moderate/high/critical 均为 0。
- `npm audit --omit=dev --audit-level=high`：0 vulnerabilities。
- 构建复制 SQL 迁移，并由 TypeScript 输出导入的政策 JSON；生产启动已实际成功。
- 本机正式服务入口：`http://127.0.0.1:3000`。单进程 worker 已恢复，使用现有忽略提交的环境配置。

合并前还需 Alex 最终报告、PM 验收和最终提交的 GitHub Actions 通过。最终 GitHub 状态与报告链接记录在 PR 及各 Issue。
