# 正式研究工作台：后端自检与集成交付

- 关联 [Issue #42](https://github.com/zequnjiang/appeye/issues/42)、[PM 需求](../requirements/RESEARCH-WORKSPACE-PRODUCTION.md)、[API 契约](../requirements/RESEARCH-WORKSPACE-API.md)。
- 日期：2026-09-09，Asia/Shanghai。当前为开发自检/独立回归阶段，尚不代替最终 PM 验收。
- CEO 负责后端、持久迁移、真实资料映射、集成与运行交付；CTO 独立负责正式前端，[自检报告](RESEARCH-WORKSPACE-FRONTEND-CTO.md)。Alex 独立测试与缺陷记录见 [报告](RESEARCH-WORKSPACE-PRODUCTION-ALEX.md)。

## 实现

008 增量迁移增加客户空间、用户/成员、一次性邀请、服务器会话、查询快照、共享关注/集合/备注/摘录、个人已读、收录申请、细分类与规则配置、审计。源市场表、原始 HTTP 及已有采集调度继续沿用。客户密码与过渡平台认证均使用带盐 scrypt；随机会话及邀请只存哈希，配置 SESSION_SECRET 后使用 HMAC 绑定会话并支持轮换撤销。

每次请求检查账户/空间/成员当前授权。平台仅管理客户元数据，不隐含客户研究读取权限；客户对旧 API 的访问也必须通过平台或已确认应用的同一授权分支。真实邮箱/密码登录、受邀新账户、已有账户验证原密码、多空间切换、降权/停用撤销会话和最后管理员保护均落库。

公共库以 confirmed 为边界，默认个人现金贷及待细分；原 loanAnalysis 回退类型不当个人现金贷证据。九字段全量过滤/排序后冻结紧凑行值，快照绑定会话及完整查询，12 小时、每会话最多 20 份，跨重启有效。probe 不自动替换行，CSV 必须使用当前快照导出其全部行，null 与 0 分开并防公式注入。自然失效明确报 410；公开范围撤销另带 SNAPSHOT_SCOPE_REVOKED，已清空记录保留有界标记，以便后续并发 probe/CSV 不误判为普通过期。

研究来源在服务器验证所属应用、源记录与原文匹配；引用正文/来源身份/原观测时间冻结保存，编辑使用 revision 防覆盖。共享研究与个人已读分别建模，历史私有研究不因公共应用退出而丢失，也不能借此访问受限原始资料。Markdown 按授权空间/集合完整生成。

收录申请复用真实任务，只有关联任务实际成功、成功详情时间满足并且当前 confirmed 才标 admitted；创建 candidate 或排队不是收录成功。规则在线编辑限定 autoConfirmStrong，版本递增，显式重分析用已有资料和原观测时间，保留 manual/legacy。

## 自检及缺陷修复

- 8 项 CEO 持久状态/HTTP 测试通过：重启与快照、跨客户/旧端点隔离、邀请重放并发/过期、角色撤销与已有账户、多页快照和九排序、来源与研究冲突、收录与规则、冻结 CSV/公式/null/撤权。日志 `.artifacts/research-csv-test.log`。
- 首轮 `npm test` **256/256 通过**，46.47 秒。日志 `.artifacts/research-full-tests.log`。此轮先于最终撤权 UI 修复，最终版本另补记录。
- Alex 独立后端阶段发现三处实际日期口径问题：泰文日期过滤、日历日期已读、无效/歧义原始日期被旧规范化值猜测补回。列表、详情和读状态现用同一可信日期证据；不能解析的原始值仍保留原文且规范日期为 null，未重写旧市场库。
- CSV 初次自检失败源于测试直接 split 逗号而忽略合法 quoted 安装区间，改为解析 quoted 单元格后 8/8 通过；不隐藏该开发中检查。

## 隔离真实资料联调与边界

`5174 → 3001` 使用只读抽取的真实当前资料副本，单独数据库，未开采集协调器；包括 2,595 个应用、每应用最近两份快照及关联变化、有限评论/来源/补充历史。测试客户和账户通过实际邀请 API 创建，仅在该隔离副本，未创建正式假客户。

真实字段联调测得副本 warm 请求应用库约 42 ms、市场活动约 309 ms，应用库 confirmed 为 506 项。以上是当时副本样本，不是全历史统计、真实整库迁移或线上性能证明。独立 4173 原型未改动；源生产 3000 仍由唯一 launchd 采集服务托管。

## 待最终集成补充

正式升级按 [运行手册](../operations/RESEARCH-WORKSPACE-UPGRADE.md) 执行：最终类型/构建/测试、停唯一采集服务、WAL 检查点、一致性全库备份、008 迁移前后旧表与固定 2,024 cohort/队列摘要、恢复服务并记录实际接口用时和采集心跳。完成后补实际结果，再交 PM 最终验收；此文暂不宣称生产升级、CI、PR 合并或 #11 原批次完成。
