# #25 稳定应用清单：Alex 独立回归

- 状态：**独立233项工程回归、正式页面稳定性及正常真实采集触发的提示/应用链路通过，已交PM最终验收。**
- GitHub：[刷新回归 #25](https://github.com/zequnjiang/appeye/issues/25)。
- 需求：[LIBRARY-REFRESH-REGRESSION.md](../requirements/LIBRARY-REFRESH-REGRESSION.md)，版本1.1/2026-09-08，LRR-AC-01–06。
- 用户已明确改选“保持当前清单，有新数据时提示，点击后更新”，此选择覆盖原 #21 的自动应用新数据要求。
- Alex仅维护 `tests/alex-library-stable*.ts`、自己的原有夹具/相关断言及本报告；不修改src/server/依赖，不构建正式dist，不操作真实数据库、服务或采集。

## 独立检查方案

1. 两个真实15秒轮询后，新/删/改行与total均只形成pending；显示成员、排序、正文、总数和分页冻结，表格DOM/焦点/阅读位置保持。
2. 点击更新或顶部手动刷新才原子接受新结果；末页失效调整到有效页，合法空结果明确呈现。失败保留旧显示及错误，随后真实成功可恢复。
3. 同值重新序列化响应不提示变化；不同query各自保存displayed/pending，切换过滤原子重置offset，过期成功/失败不得覆盖当前query。
4. 打开详情后原displayed与pending都保留，返回恢复筛选/页码/阅读位置/入口焦点，不因最新缓存自动替换旧清单。
5. 817×863顶部插入新首行：后台变化不得造成scrollY0→90，也不得让标题或筛选移动；明确点击应用后仍保留控件区位置。
6. 首次无缓存、未变化、pending、检测错误、显式更新中和成功状态分别核对。退出会话清理及20查询缓存边界保留原回归。

浏览器用随机端口Vite、真实Chrome和全部API拦截，禁止外部请求；CTO报告与合成顶端红用例不冒称正式后台已复现。正式部署后的817×863至少两轮实际轮询由CEO部署后提供/授权只读核查，工程与运行证据分开记录。

## 实施中独立预检

`node --import tsx --test tests/alex-library-stable-cache.test.ts` 4/4通过。覆盖displayed与最新pending分离、同值/对象字段顺序等价、applyPending不发网、手动刷新与进行中请求合并且成功才应用、后台/手动失败保留两份快照、会话清理迟到错误、应用库以外页面原行为及统一有界淘汰。

浏览器首轮 `node --import tsx --test tests/alex-library-stable-browser.test.ts` **2/3通过、1失败**（日志 `.artifacts/alex-library-stable-browser-precheck.log`）。817×863顶部新行/删除/内容和总数变化不推移控件/表格、390px详情返回displayed+pending与正文锚点两项通过。

独立发现的实际实现缺陷：先缓存第1页120条总数，进入第6页后服务器只剩20条；后台冻结旧页正确，但点击更新并回退到第1页时重新使用该页旧显示缓存，total仍120，5秒内没有接受实际20，必须额外点击。已交CTO修复显式应用跨有效页回退的语义；原断言保留，尚待复测。这个失败来自隔离夹具，不是生产数据库问题。

## 正式工程回归与缺陷闭环

收到 [CTO正式自检](LIBRARY-REFRESH-REGRESSION-CTO.md) 于2026-09-07 18:02 UTC冻结交接后，Alex独立运行：

```text
npm test
npm run typecheck
git diff --check
```

结果 **233/233通过，0失败、0跳过，44.856467秒**；前后端类型及diff检查通过，命令退出码均0。对应提交 `a7a9f2f8d650b1c8249a3764e166b8c1df77087a`，本机Node v25.9.0。记录于 `.artifacts/alex-library-stable-engineering.json`；完整测试日志SHA256为 `69944c5211a95a00a16d7811d48d707991ac81ef7bb6cd27f1dc3a2a153e655a`。Alex没有构建或替换正式dist。

本轮新增Alex独立用例7项：4项缓存边界与3项真实Chrome浏览器夹具。既有 `alex-silent-browser` 的真实两轮15秒用例按用户新选择改为“后台变化暂存→点击采用最新”，原DOM、分页、焦点、失败和恢复断言保留。旧 #21 文档与报告未改写历史结论。

| 独立验证 | 实际覆盖 |
| --- | --- |
| 持久显示与待应用 | 后台改变成员、排序、行内内容与total，显示快照仍逐字段相同；最新pending替换旧pending，同值/对象字段重排不提示，结果回到原值时撤销提示。 |
| 明确应用与失败 | applyPending不发请求；显式刷新共享单次请求且成功才应用；已有displayed/pending在后台或显式失败后均保留。恢复后的后台更新重新暂存，不能把一次点击变成永久自动应用。 |
| 查询、详情与会话 | 390px非首页详情返回恢复原行、total、pending、国家、阅读位置及app入口焦点；跨市场无pending污染。旧请求/退出会话、双快照有界淘汰、其他页面继续自动更新均覆盖。 |
| 顶部与阅读位置 | 817×863顶部在无变化、新行/删除/文本/总数变化时，scrollY、标题及表格位置保持；提示不推移表格。显式采用后仍在顶部；正文采用新排序时保留可存续行相对位置。 |
| 末页原子回退 | 未点击时第6页和旧总数保持。点击后目标有效页被延迟，仍保持完整原显示；目标503时旧显示和pending继续保留，再次点击成功后行/total/page同时切换；显式刷新到合法空结果不循环请求。 |
| 生命周期及旧功能 | 独立全套重跑CTO隐藏/恢复、单次慢请求、历史展开和诊断等场景；无正式API拦截数据被误称采集成果。 |

末页缺陷的第一次修复仅让目标页请求继承显式应用。Alex进一步用可控延迟证明：成功前会短暂显示旧第一页的1–20/total120；此第二次红证据保留于 `.artifacts/alex-library-stable-page-recovery-precheck.log`。最终CTO改为先取得有效页成功快照，再切显示与offset。原3项浏览器复测3/3通过；Alex又在同一末页用例中增加目标页503与重试成功，1/1通过，最终包含在233项内。没有删除或降低原断言，预检失败不被冒称为一次全绿。

合成截图 `.artifacts/alex-library-stable-817-pending.png`、`-817-applied.png`、`-390-return-pending.png` 留存本机；Alex已实际查看817提示截图。它们使用真实浏览器、拦截API的合成场景，不作为LRR-AC-06生产链路证据。

## 正式页面证据边界

CEO先前提供旧部署817×863泰国页33秒观察；Alex只读核对其JSON：133个采样、同一表格、scrollY仅0、无表格移除/加载闪空/JS错误。这段窗口未出现首行变化，不能据此宣称正式页已复现CTO的0→90缺陷。完整旧部署证据由CEO保留，和合成红用例分开。

LRR-AC-01–05的工程范围通过。以下补充客户端切换后的LRR-AC-06实际认证页面结果。PM最终验收前不关闭#25，#11/#24不在本轮完成范围内。

## 正式页面独立验证

CEO于2026-09-07 **18:05:42.620–18:05:42.658 UTC**原子替换正式客户端，采集器PID3848保持。Alex于18:07:03独立核对11个源文件与提交、11个正式构建文件与发布清单全部逐字节相同，PID存活，`errors=[]`；报告为 `.artifacts/alex-library-deployment-audit.json`。浏览器实际加载 `index-ZLBOd48d.js`，与提交 `a7a9f2f8d650b1c8249a3764e166b8c1df77087a` 的隔离构建对应。

Alex使用真实Chrome、817×863、独立认证会话和正式 `/api/apps?country=th&limit=20&offset=0`。所有API响应保持原样，只有远程装饰图标请求被测试阻断；未替换任何市场响应。Alex仅登录/退出会话、切筛选、读取、详情返回和点击界面更新，没有发采集任务、改设置/数据库或重启服务。

### 第一段：自然窗口，未发生新结果

第一段于18:06:30–18:09:35执行，观察窗口153秒、613采样。泰国显示324条、第1页20条，实际TH API正文SHA始终相同；全部观察样本保持原显示、顶部和同一表格，未移除表格、闪加载或重新导航，**没有自然pending**。不据此宣称“真实新数据提示链路已通过”。

原 `.artifacts/alex-library-stable-live.json` 保留1项探针断言错误：详情返回时AppIcon重新挂载，被阻断的远程图标尚未触发onError，占位首字母暂缺；直接比较整行innerText把装饰生命周期当作资料变化。原20身份、业务来源SHA、返回Y386、焦点和TH条件均保持。后续探针仅移除明确 `.app-icon` 装饰子树后比较其他全部业务DOM文本，不trim、不忽略业务字段、不修改产品代码。原错误报告不覆盖，不把首轮说成全0。

### 第二段：CEO一次正常真实采集触发

第二段保留独立文件。Alex于 **18:12:11.780** 完成泰国同查询、原显示快照的baseline，之后始终保持该页。CEO收到baseline后，对当前页既有 `ihappyloan`（TH/GP，ID2089，包名 `com.lethai.all`）提交**一次正常详情refresh**；未新增身份、插假数据或手写市场资料。此段为“CEO正常真实采集触发”，不称自然市场变化，也不声称应用刚发布新版本。

| 实际时间与证据 | 独立核查结果 |
| --- | --- |
| 18:13:05.708 job1312入队；18:13:07.215成功资料 | 真实lastFetchedAt由17:32:52.667变为18:13:07.215，任务1次尝试成功。manual response19已应用，snapshot5184与HTTP3078为官方TH/th详情HTTP200。 |
| 18:12:11.780–18:13:14.961，63秒/253采样 | 4次实际15秒TH检测。所有采样的20行身份/顺序/全部业务文本、总数和分页哈希保持原快照，标题/表格位置及scrollY0保持，搜索焦点保持，表格移除0、加载闪空0。 |
| 18:13:11.596后台新结果 | API正文SHA确实由旧值变为新值；当前显示SHA仍保持原值，仅出现“有新数据，当前清单保持不变”和“更新清单”。真实提示截图已由Alex查看。 |
| 18:13:15.039点击采用 | 提示消失，采用的20行ID/标题/总数逐项等于最新成功API快照；显示业务哈希改变，scrollY仍0。实际变化来自正常采集时间更新，不能推断版本或市场内容必然改变。 |
| 正文详情返回 | App2087的相对位置Y386→386，返回仍为TH及原显示快照，焦点回到app-2087；整个浏览器链路只有1次document导航。该次返回发生于应用后；“带pending返回”的确定性边界另由合成用例覆盖。 |
| 18:13:15.288–18:13:15.755顶部显式刷新 | 回到顶部后点击“刷新当前数据”，与返回时已在途的同query GET合并，成功响应被应用，ID/总数与来源一致，提示清除且scrollY0；不误称额外发起了第二个同键请求。 |

第二段 `.artifacts/alex-library-stable-live-triggered.json` **退出码0、errors=[]**。点击前显示SHA为 `e20d6804b6561324e869d5b653715540fddccf9ad82b7c266fcb17c2f76fe529`，点击后为 `996c01602eba0dca45f43e4d87ca0137fbc9d62eddbdb855154fe9340112dd51`；最新API来源SHA为 `bf2a9b5bf4bc98b06ead2eb6adf4a88bea2f612c52eb60b63486408423b05005`。截图 `-start/-observed/-applied/-returned.png` 保留在同一本机忽略前缀下，截图均来自这次正式页面。

Alex于 **18:14:48.285–18:14:48.293** 又对该有限来源链只读复核：job1312、response19、snapshot5184、HTTP3078和当前资料的身份/data/raw/时间逐字段匹配，原classification/firstSeen保持；真实来源发生在浏览器baseline之后、新API和点击之前。结果 `.artifacts/alex-library-real-update-audit.json`，`errors=[]`。正常采集执行归属CEO，Alex的作用为独立观察与验证，没有额外商店请求。

## 最终交接

LRR-AC-01–05的确定性工程范围及LRR-AC-06正式认证页面、两个以上真实周期、资料更新提示/明确应用、正文返回和顶部刷新链路均已独立核查，交PM逐项最终判定。自然153秒未变化、装饰图标探针误报、CEO正常采集触发、合成边界与真实页面证据分别保留；本结论不延伸为#11全量采集或#24来源兼容完成。
