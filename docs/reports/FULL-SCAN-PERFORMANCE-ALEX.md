# Alex：持续评论分页的统计成本检查

2026-09-06 19:24 UTC，独立只读检查正式库查询计划，并在临时内存 SQLite 中构造最多 50,000 页/HTTP 和 1,000,000 条评论身份。未改生产源码/数据库，未新增商店请求。脚本 `.artifacts/alex-summary-performance.mjs`；测量记录 `data/batches/finance-2026-09-07-alex-summary-performance.json`。

**当前没有阻塞证据，但每页重新计算全批统计的成本随数据增长。** 当前正式库还未开始评论阶段，8382 条 HTTP 的 COUNT 暖读中位 5.15ms；评论新增/更新 SUM 即使结果为 0 仍耗 9.27ms，因为先扫描全部 succeeded 任务再筛选 reviews。kind/status 分组另耗 3.69ms。这里只测四条关键查询，不把其合计误称整个 `summary()` 的完整耗时。

查询计划：HTTP 的 batch COUNT 是 `SCAN full_scan_http`；review_seen 已使用以 batch_id 开头的覆盖主键，无需重复添加相同索引；review SUM 使用 `(batch_id,status,...)` 队列索引后读取任务行与 JSON；任务分组使用覆盖索引并建立临时分组树。CLI 每完成一次任务就调用全量 summary/publish，尽管控制台日志只每 15 秒输出一次。

| 合成成功页 / HTTP 数 | 评论身份 | seen COUNT | HTTP COUNT | review SUM | 任务分组 | 四项合计 |
| --- | --- | --- | --- | --- | --- | --- |
| 1,000 | 20,000 | 0.24ms | 0.05ms | 0.14ms | 0.13ms | 0.57ms |
| 10,000 | 200,000 | 2.87ms | 1.59ms | 2.07ms | 1.42ms | 7.94ms |
| 50,000 | 1,000,000 | 17.25ms | 11.65ms | 11.34ms | 7.77ms | 48.01ms |

表内为同机内存库五次暖读的中位数，HTTP body 为合成 2 KiB。真实大响应、磁盘冷读、并发 UI 和缓存命中率均会影响结果；该样本不证明实际未来会采得多少评论。现有 901 个 GP listing 的 reviewCount 总和是来源指标，不能用作实际分页目标、待采数量或时长预测。

只在合成库添加 `full_scan_http(batch_id)` 索引后，50,000 行 COUNT 改为覆盖索引，11.65ms 降至 0.57ms；这是明确可测的最小改善。若 CEO 决定为持续分页准备优化，建议优先此索引；其次把 CLI 全量统计发布降为约 15 秒一次并在启动/退出强制刷新，任务退出条件仍用准确的轻量查询。不要仅缓存旧 summary.pending 决定是否结束。当前没有依据要求立即增加复杂持久增量计数器；后续实际分页成本超过现有请求间隔的一部分时，再依据测量处理 review SUM 或计数维护。

此报告只提供工程性能证据和建议，不包含任何生产优化操作，也不改变采集范围、页数上限或完整性口径。
