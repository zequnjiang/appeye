# 2026-09-20 开放 Issue：CEO 受控发布

范围：原 15 个开放 Issue 加正式验证发现的 #49，共 16 项；见 [PM 需求](../requirements/OPEN-ISSUES-20260920.md) 与 [运行增量需求](../requirements/ISSUE49-RUNTIME.md)，实现 [PR #48](https://github.com/zequnjiang/appeye/pull/48)。当前正式代码 `7e2d745`，完整回归 **333/333**、类型与隔离构建通过。下文保留各次发布和失败的真实顺序；最终逐项结论以 Alex / PM 报告为准。

## 工程检查

- `npm test`：第一次功能发布 **309/309** 通过，0 skipped / failed，47.2197 秒。包含 Alex 独立 16 项和 CTO 的 #46 完整历史补修。
- `npm run typecheck`、前后端隔离构建和 `git diff --check`：通过。
- `npm audit --omit=dev --audit-level=high`：0 vulnerabilities。
- 新前端资源 `index-DFVfwQzx.js`，构建在独立暂存目录，在线旧服务在切换前未读取正在构建的文件。

## 正式资料维修

停止唯一 Appeye launchd collector 并等待其退出后，WAL checkpoint TRUNCATE，创建第二份独立 APFS clone：`data/backups/appeye-before-2026-09-20-release.sqlite`，**107,135,963,136 字节**，0600。2026-09-20 04:15:49Z 完成；文件大小、独立 inode、业务表哈希与首/中/末字节样本一致。它是完整数据库备份；验证未冒称整文件 SHA。首次 bootout 尚在退出时维护守卫拒绝执行，确认退出后才重试。

随后离线更新 **3,563 / 3,563** 条旧识别分析；没有缺失原源、没有网络请求。旧/新分析逐项写入研究审计。维修前后所有应用事实（除 `loan_analysis`）、有效分类、六国配置、私有研究表、原批次 **2,024** 个成员和历史原始表高水位哈希保持一致。

原始历史和人工决定没有被新规则改写。后续新观测仍由正式采集器按正常调度产生。

## 来源恢复

- #11：第一份备份后仅重试已核对的原 **165** 个 GP 评论失败任务；保留原 app 身份、page、cursor、旧 attempts/HTTP，不扩展原 2,024 成员。恢复中部分原游标返回真实 HTTP 200 的 `UsvDTd code 5`，未解释为成功空页或证明下架。独立当前入口样本 app 1236/job 1318/manual response 37 成功保存 **150** 条源评论，只是当前样本，不能替代历史流终验。
- #24：按现有运行 cycle 11 的原预算新增 5 个独立类似应用验证任务 **113299–113303**，分别引用原 task 129/233/337/438/524。未改旧任务、未开新周期、未重置预算。通过原公平调度及共享限速执行；实际结果待追加。

## 发布核对

暂存 release 切换为 `dist`，旧 release 留在私有忽略目录供回退。2026-09-20 04:16Z 重新启动同一 `com.appeye.collector`，PID 84942。恢复期间需要确认 Appeye 自身就绪：仅 HTTP 200 不足，因为同机另有无关 IPv6 3000 服务；启动未就绪时的首次探针命中了该服务的 HTML，未当作通过，后续先核对正式资源身份再登录。未停止或修改无关服务。

正式 API / 浏览器、#24 新来源、#11 最终状态和 PM 验收待后续追加；本段不表示所有 Issue 已完成。

2026-09-20 04:19Z 服务完成恢复，确认 PID 84942 实际监听 `127.0.0.1:3000`，新版资源身份正确。CEO 对正式接口的八个历史问题应用（373/1076/1177/999/1000/1807/116/2089）核对全部通过：heuristics-3、真实原文 offset、32–40 APR上限、Maya储蓄收益、泰文商店日期、完整历史隐私DTO均符合；冻结CSV三类列及GCash待细分、20条手动任务身份返回正常。证据保存在私有 `.artifacts/issues-20260920/production-api.json`；未将评论正文或凭证加入仓库。

代码 `bd5c171` 的 GitHub Quality gates 两项均 SUCCESS（[运行记录](https://github.com/zequnjiang/appeye/actions/runs/35488680910)）。报告后续提交仍须检查最终 HEAD CI，不能沿用本次提交的绿灯合并不同 HEAD。

## #49 发布阻断与修复

Alex 三次正式页面复验保留了首页/诊断超时，暂停其大型审计后仍出现。没有提高超时或把空白截图当通过。新增 [#49](https://github.com/zequnjiang/appeye/issues/49) 和 [RUN01–04 需求](../requirements/ISSUE49-RUNTIME.md)，阻止此前13功能提前验收。

临时SQL追踪定位到同步汇总与重复资料读取，SQL日志仅在私有忽略目录、不含参数/结果。主要瓶颈是每15秒进度汇总反复遍历 **7,378,102** 个seen身份，独立真实库副本单次COUNT **7.912秒**；候选汇总实际1.9秒，市场重复读、诊断先搬运大raw再分页也叠加阻塞。修复保持精确数据和公平调度：首次事务汇总与触发器计数、候选覆盖索引、市场单次事件遍历、诊断先选轻量分页身份再取当页raw。各模块旧/新结果深等与独立测试见CTO/Alex报告。

正式代码 `cb6ff99ac51f1459c4cb114a59b542130347573e`：完整回归 **322/322**、0 skipped/failed、48.004秒；Alex另跑99项相关回归。types、隔离构建通过。

2026-09-20 04:43:44Z 停止唯一collector后完成第三份独立完整备份：`data/backups/appeye-before-2026-09-20-runtime.sqlite`，**107,490,242,560字节**、0600，大小/独立inode/表哈希/3个字节样本核对通过。受控建立索引及派生计数，并再直接COUNT原seen核对，共17.406秒；结果 **7,378,102** 完全一致，重复初始化0.661ms不改变值。维修前后所有apps字段（这次含loan_analysis）、国家、私有研究、原2024cohort及历史高水位保持一致。

04:44Z 安装新release，原launchd配置逐字恢复并移除临时trace，PID **93190** 恢复正常采集；确认Appeye health JSON200和IPv4实际监听。仅清理本轮隔离预览3001/5174，未操作无关应用或原型4173。后续正式连续响应与页面核对结果由Alex追加，当前段本身不作为RUN03/04通过证据。

修复后 Alex 原正式流程11项通过，217个部署文件与暂存产物逐一SHA一致；但首轮RUN03严格采样仍 **13/14**，一次首页请求在GCash搜索/冻结CSV期间超过2秒。该轮保留为失败，没有放宽阈值；已继续缩减应用库先读取所有confirmed大JSON再过滤的路径，待下一轮真实验证。不能把正式页面可用或后半段快速响应替代RUN03门槛。

## 应用库剩余路径补修

CTO将应用库改为先筛身份/类别元数据，再投影匹配应用的展示字段，保留原JS Unicode搜索、日期/空值类型、排序及冻结导出语义。旧模块完整DTO和序列化深等；真实资料副本的GCash查询约479.5→7.1ms，无搜索678行59.3→26.8ms。该计时只是副本对照，不代替正式运行门槛。

最终完整回归 **323/323**、0 failed/skipped、51.2455秒；类型检查、隔离前后端构建通过。首次全套新增专项因把国家列表第一项假定为非MX而失败，修正为读取夹具实际国家并选另一个国家，未改生产代码或删业务断言，原失败日志保留。

正式代码 `52ca92b` 于2026-09-20 04:56:57.850Z仅切换代码：停止并确认唯一collector及DB句柄退出后，将217份暂存文件核SHA再原子切换dist，cb6ff99产物另留回退。没有再次维修数据库或改队列。PID **97262**恢复正常采集，Appeye IPv4监听、前端资源身份和health JSON均核对通过；原launchd配置逐字一致、无临时trace。等待Alex同一正常collector下的65秒持续响应及正式完整流程复验。

## 2026-09-22 中断恢复核对

恢复时本地/远程HEAD均为52ca92b，PR48仍draft，16个Issue均未关闭；最后两项后端增量尚未发布。原collector继续采集，临时get/all/run慢SQL诊断因9月20执行中断仍在运行。恢复后先核对原配置除该import外无其他变化，再停唯一collector，逐字恢复原plist；首次bootstrap早于退出完成而拒绝，确认服务及DB句柄已退出后成功恢复。当前代码仍52ca92b、PID45642、正确Appeye资源与health live JSON核对通过，无临时trace。诊断日志126,998,803字节留在私有忽略目录，不把带trace期间的计时作为最终验收。

9月20曾交接的测试/源审计作为历史时点证据保留；Alex重新校验其摘要并另做当前原批次小范围终态核对。9月22最终补修需重新独立测试、迁移、正式65秒运行验证和PM验收，不能沿用两天前的服务状态。

## 9月22最后查询补修受控发布

正式源码 **1cf0eea**：候选同步仅写身份/状态确实变化的行，队列先按原规则选ID再读完整记录；010覆盖版本索引与来源写入PK存在性检查一起交付。CEO最终完整回归 **329/329**、0 failed/skipped、48.16676秒；typecheck、隔离构建、diffcheck通过。Alex独立受影响118/118及类型检查通过，CTO自检及等值证据见对应报告。

07:03:48.952Z停止唯一collector且无DB句柄后，完成新的独立完整COW备份 `data/backups/appeye-before-2026-09-20-collector-hotpaths.sqlite`，**208,853,356,544字节**、0600。文件名保留本轮任务日期；实际创建时间为9月22日。独立inode、大小、业务摘要与首/中/末字节样本一致，不宣称逐字节全文件哈希。

010及Store初始化共 **25.361秒**，版本查询实际EXPLAIN为COVERING INDEX；既有批次schema重复初始化0.064ms。维护前后所有apps字段、分类、国家、私有研究、原2024成员及历史高水位摘要均一致。该次只新增查询索引，没有再次重分析或重新排队。

随后原子安装218份发布文件，52ca92b产物留作回退；原launchd配置逐字不变，PID **49023**恢复唯一Appeye IPv4服务，实际首页资源及health live JSON正常，未碰同机无关IPv6服务。正式RUN03/04由Alex继续验证，不用迁移/单次health成功替代持续运行门槛。

## 空暂停目标扫描增量

1cf0eea的正式11流程通过，但同窗65秒仍有两次2秒超时，保留为失败。进一步核实：六国全启用时，两个scheduler仍每轮执行暂停国家队列UPDATE。扩展队列计划为全表SCAN，完整备份副本的0匹配操作仍429.326ms。现在在原事务内先检查countries小表，只有确有暂停国家才执行原UPDATE；没有缓存国家配置或更改暂停/恢复语义。

最终源码 **7e2d745**，全套 **333/333**、0 failed/skipped、52.544秒；types、隔离构建通过。Alex独立122/122与类型检查通过。停止唯一collector并确认DB句柄退出后，仅替换218份代码文件，保留1cf0eea产物回退；无新DB迁移或任务重排。PID **52666**恢复原配置、无trace，首页资源与health live JSON正确。最终持续响应与正式操作结果待Alex，前三轮失败继续保留。

7e2d745最终正式复验：Alex在07:15:25.421–07:16:30.435Z持续 **65.009秒**，首页与health各7次、总14次全部正确Appeye200，最大 **829.71ms**、0超时，原2秒门槛未变。同PID52666及锁身份保持、heartbeat推进，monitor HTTP增加24、response增加443（两者口径不同，不能当作同一计数）。同窗正式页面11项全部通过，诊断→详情→返回和GCash冻结CSV包含其中。之前三轮不通过记录完整保留。本段采样执行归Alex，不冒称CEO重复执行。

最终交接已完成：Alex核对218份实际产物与19份关键证据，PM独立复核后签署16项通过（#11明确按来源限制完成）。[PM最终判定](OPEN-ISSUES-20260920-PM.md)允许CEO在最终报告提交的精确HEAD CI全部成功后合并并关闭对应Issue。最终CI及合并/关闭时间以PR #48和各Issue的GitHub记录为准，避免沿用先前HEAD或在报告中预写未执行结果。
