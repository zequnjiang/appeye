# 评论网络失败：Alex 只读诊断与恢复基线

- 关联：[采集运营 #11](https://github.com/zequnjiang/appeye/issues/11)；FS-AC-05/06/08/09/10/11 的分页、历史、有限恢复和完成边界。
- 源码基线：runtime-7 / `52cd7a8`。本次 Alex 只读查询于 **2026-09-07 07:35:29–07:37:49 UTC** 执行，无生产写入、无商店请求、未操作进程或触发重试。
- 诊断结论：7 个评论页的 21 次尝试均在取得 HTTP 响应前失败；现有证据支持网络连接故障，不能认定为接口无数据或永久不可用。保存的续页检查点完整，适合由 CEO 在确认单一 writer 后安排 **仅这 7 页**的新一轮有限恢复。实际恢复结果尚待单独核查，本报告不标为已恢复或全批次完成。

## 实际状态与原因

`07:35:29Z` 的数据库快照为 Apple **3,803 succeeded**；Google Play **27,040 succeeded、233 queued、7 failed**，未发现 running / deferred 评论任务。最新已完成 attempt 为 **51871** / task **51672**，`04:19:39.895–04:19:40.529Z` 成功，比此前 `04:19:39` 状态文件多一个成功页。进程、锁和系统日志由 CEO 独立调查，SQL 最后完成时间本身不能证明进程停机原因。

| 失败 task | App | 国家 / 商店 | 页码 | 已完成失败尝试 |
| --- | ---: | --- | ---: | ---: |
| 49999 | 1228 | 印尼 / Google Play | 71 | 3 |
| 50000 | 1229 | 印尼 / Google Play | 71 | 3 |
| 50001 | 1230 | 印尼 / Google Play | 71 | 3 |
| 50002 | 1234 | 印尼 / Google Play | 71 | 3 |
| 50003 | 1235 | 印尼 / Google Play | 71 | 3 |
| 50004 | 1236 | 印尼 / Google Play | 71 | 3 |
| 50005 | 1237 | 印尼 / Google Play | 71 | 3 |

七项首次尝试集中在 **01:49:01.624–01:49:30.822Z**；后续有限重试延续到 **02:23:52.576Z**。21 条 HTTP 全部 `status=null`、`content_type=null`、`body=null`，具体底层错误为：

- **20 次 `ECONNRESET`**：在安全 TLS 连接建立前，客户端网络连接断开。
- **1 次 `UND_ERR_SOCKET`**：连接另一端关闭，位于 task 49999 的首次尝试。

各页均有原 3 条 attempt 和 3 条 HTTP 错误记录；没有持久成功 response，`response_at/result/stop_reason` 为空，没有将失败写成空页、自然结束或接口上限。它们处于共同网络故障时段，后续其他页仍成功到 04:19:40；该证据不能推出这七个 App 的评论源永久不可用，也不能证明当前网络已经恢复。

## 保存的可恢复点

`07:37:49.875–07:37:49.901Z` 独立核对七条流：

- 每个 App 的 **第 1–70 页均为 succeeded，共 70 页**，没有此前页缺口。
- 第 70 页成功响应中的 nextCursor 与当前第 71 页 payload 保存的 cursor 完全相同；请求国家和语言均保持 `id`。
- 当前失败页的完整 payload、cursor 以及前一成功 response 已保存 SHA256，供实际恢复后对照；报告不导出原游标或评论正文。
- 当时 **233 个 queued 评论页均已到期**，应由现有恢复流程继续，不应静默删除或称为已结束。

原 12 个已验收来源限制仍全部是 **App Store enrich failed**，与本次 Google 评论网络错误分开。源码 `retryFailed()` 按本 batch 的全部 failed 重排，没有 kind / ID 限定；直接使用现有 `--retry-failed` 会同时重排这 12 个 Apple 补充任务。

因此恢复应严格限定上述七个 task，沿用原游标和累计 attempt，记录新一轮预算基数为原 attempts=3，并保留原 21 条 attempt / HTTP。新真实响应另存，成功页不重抓，其他 233 个 pending 正常继续。若再次失败仍按原有限预算记录，不自动无限开启新轮，也不把失败强行改成 empty / unsupported。该操作由 CEO 执行，Alex 本次未执行。

## 本地证据与后续验收

- `data/batches/finance-2026-09-07-alex-review-failures-20260907-0734.json`：实际状态、七任务及全部 21 条 attempt / HTTP 元信息和原因、最近完成记录；查询时间为 `07:35:29.390–07:36:05.537Z`，文件名标识任务发起时段。
- `data/batches/finance-2026-09-07-alex-review-recovery-precheck-20260907.json`：七条流的前页连续性、原游标对应关系及哈希、pending 到期时间和其他失败范围；错误 0。

下一步实际 QA 需核对：仅七目标被重排、原累计 attempt / HTTP / 已成功页保持、cursor 和国家语言保持、实际新 attempt / response 的来源时间正确、成功后继续下一页或有真实合法终止证据；若预算耗尽则仍明确失败。12 个 Apple 补充限制和 Apple 评论阶段结果不得被此次恢复改变。尚有 233 个待续页及七个未恢复失败，不能批准 #11 整体验收。
